- name: Create docker directory
  file:
    path: /data/apps/adminer
    state: directory
    owner: docker
    group: docker
    recurse: true
    
- name: copy docker-compose.yml
  template:
    src: apps_adminer_compose.yml
    dest: /data/apps/adminer/docker-compose.yml
 
- name: Run docker-compose
  shell:
    docker-compose up -d
  args:
    chdir: /data/apps/adminer
    
- name: Create a symbolic link
  file:
    src: '{{item.src}}'
    dest: '{{item.dest}}'
    state: link
  with_items:
    - {src: /data/apps/adminer/docker-compose.yml,dest: /data/config/apps_adminer_compose.yml}
    - {src: /data/apps/adminer,dest: /data/wwwroot/adminer}
