- name: Create docker directory
  file:
    path: /data/apps/phppgadmin
    state: directory
    owner: docker
    group: docker
    recurse: true
    
- name: copy docker-compose.yml
  template:
    src: apps_phppgadmin_compose.yml
    dest: /data/apps/phppgadmin/docker-compose.yml
 
- name: Run docker-compose
  shell:
    docker-compose up -d
  args:
    chdir: /data/apps/phppgadmin
    
- name: Create a symbolic link
  file:
    src: '{{item.src}}'
    dest: '{{item.dest}}'
    state: link
  with_items:
    - {src: /data/apps/phppgadmin/docker-compose.yml,dest: /data/config/apps_phppgadmin_compose.yml}
    - {src: /data/apps/phppgadmin,dest: /data/wwwroot/phppgadmin}
