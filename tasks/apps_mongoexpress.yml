- name: Create docker directory
  file:
    path: /data/apps/mongoexpress
    state: directory
    owner: docker
    group: docker
    recurse: true
    
- name: copy docker-compose.yml
  template:
    src: apps_mongoexpress_compose.yml
    dest: /data/apps/mongoexpress/docker-compose.yml
 
- name: Run docker-compose
  shell:
    docker-compose up -d
  args:
    chdir: /data/apps/mongoexpress
    
- name: Create a symbolic link
  file:
    src: '{{item.src}}'
    dest: '{{item.dest}}'
    state: link
  with_items:
    - {src: /data/apps/mongoexpress/docker-compose.yml,dest: /data/config/apps_mongoexpress_compose.yml}
    - {src: /data/apps/mongoexpress,dest: /data/wwwroot/mongoexpress}
